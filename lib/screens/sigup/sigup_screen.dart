import 'package:flutter/material.dart';
import 'package:rioappone/screens/sigup/components/body.dart';

class SigupScreen extends StatelessWidget {
  const SigupScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Body();
  }
}
